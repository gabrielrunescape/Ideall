<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/coluorPrimary</item>
        <item name="colorAccent">@color/coluorAccent</item>
        <item name="colorPrimaryDark">@color/coluorPrimaryDark</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/coluorIcons</item>
    </style>
</resources>
